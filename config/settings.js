export const projectName = "hlaup"

export const port = 8808;
export const publicPath = `/content/${projectName}/`; // Production mode on server (If testing production Locally it should be /Umbraco.Site/content/hlaup)
export const minifyDevelopment = true;

export const deployPath = `/Umbraco.Site/content/${projectName}/`